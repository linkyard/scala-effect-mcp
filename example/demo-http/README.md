# Demo MCP Server (HTTP)

Same as the [Demo MCP Server (Stdio)](../demo/README.md), but using streamable HTTP instead of stdio.

Source: [HttpDemoMcpServer.scala](src/main/scala/ch/linkyard/mcp/example/demo/HttpDemoMcpServer.scala)

The server is available under <http://127.0.0.1:18283/mcp>.
